{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      "@/services": ["./services"],
      "@/types": ["./types"],
      "@/utils": ["./utils"]
    }
  }
}
